# hate-speech

## Setup

This repo uses Git LFS to store large dataset files. Install it before cloning:

```bash
brew install git-lfs
git lfs install
```

Then clone as usual — LFS files will download automatically.

If you already cloned without LFS, run:

```bash
git lfs pull
```